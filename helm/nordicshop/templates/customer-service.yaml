apiVersion: v1
kind: Service

metadata:
  name: customer-web
  namespace: {{ .Release.Namespace }}

spec:
  selector:
    app: nordicshop-customer-web

  ports:
    - port: 8080
      targetPort: 8080