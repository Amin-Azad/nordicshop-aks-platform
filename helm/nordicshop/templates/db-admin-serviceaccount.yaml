apiVersion: v1
kind: ServiceAccount

metadata:
  name: nordicshop-db-admin
  namespace: {{ .Release.Namespace }}

  annotations:
    azure.workload.identity/client-id: {{ .Values.azure.dbAdminClientId | quote }}
